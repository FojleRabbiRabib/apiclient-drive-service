# Changelog

All notable changes to this package are documented here. Format loosely follows
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/). Every entry generated by the sync
pipeline records the exact upstream commit it was synced from.

## [Unreleased]

### Added
- Project scaffolding: `composer.json`, `LICENSE`, `NOTICE`, `.gitignore`, `.gitattributes`,
  `.editorconfig`, `README.md`, and the `src/`, `tools/`, `.github/workflows/` directory
  structure.
